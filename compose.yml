services:
  converter:
    image: ghcr.io/vladtraistaru/converter:latest
    container_name: converter
    restart: unless-stopped
    networks: [proxy]
    labels: ["com.centurylinklabs.watchtower.enable=true"]
networks:
  proxy:
    external: true
